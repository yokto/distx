pathadd() {
    if [ -d "$1" ] && [[ ":$PATH:" != *":$1:"* ]]; then
        export PATH="$1:$PATH"
    fi
}
pathadd "/home/silvio/stuff/sources/elfx86/llvm/bin"
gdb () {
	(cd ..; command gdb /home/silvio/stuff/sources/elf/build/load_elf)
}
ph () {
	echo "#include <$1>" | gcc -x c -E -  -o -
}
