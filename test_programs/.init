pathadd() {
    if [ -d "$1" ] && [[ ":$PATH:" != *":$1:"* ]]; then
        export PATH="$1:$PATH"
    fi
}
pathadd "/home/silvio/stuff/sources/elf_src/llvm/__zwolf_build__/llvm/bin"
ph () {
	echo "#include <$1>" | gcc -x c -E -  -o -
}
